apiVersion: v1
kind: ConfigMap
metadata:
  name: secureflow-config
  namespace: secureflow
data:
  # CK-09 remediated — secrets removed from ConfigMap.
  # All credentials now delivered via Vault Agent Injector.
  # See infra/kubernetes/overlays/dev/ for Vault annotations.
  AUTH_SERVICE_URL: "http://auth-service:5001"
  TRANSACTION_SERVICE_URL: "http://transaction-service:5002"
